# Optional — MicroK8s Traefik ingress. Edit host and apply separately:
#   kubectl apply -f 06-ingress.yaml
#
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: kiko
#   namespace: kiko
#   annotations:
#     kubernetes.io/ingress.class: public
# spec:
#   rules:
#     - host: analytics.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: kiko
#                 port:
#                   number: 8080
