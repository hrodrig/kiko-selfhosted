apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiko.fullname" . }}
  labels:
    {{- include "kiko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiko.selectorLabels" . | nindent 4 }}
